
## What we need to do today :
-- dynamic container, constexpr array wrapper, stack, queue.

## What needs to be done tomorrow :
-- a memory manager (handles freeing)
-- A string wrapper, slice<T>, and 3 Allocators (Bump, Pool, ...)
-- Simple Random Number Generator, print with different levels.
-- A timer.
